local function loader(Api)
    Api:CreateCharacter("mod-skin", "naysayer")
    local character_data = Api:GetCharacterData("mod-skin")
    if character_data == nil then
        return
    end
    character_data.icon = Api.Texture("character_reskin")
    character_data.icon_low_resolution = Api.Texture("character_reskin_high_res")
    character_data.icon_high_resolution = Api.Texture("character_reskin_high_res")
    character_data.icon_high_resolution_size = { width = 700, height = 700 }
    character_data.unit = Api.Unit("COMBAT Unit Mod Reskin Character")

    Api:RegisterMissile("mod_attack_rock", {
        id = "mod_attack_rock",
        display_name = "CASH!",
        tip = "Deal <color=green>88</color> damage",
        icon = Api.Texture("custom_attack_cash"),
        missile = Api.SimpleUnit("COMBAT Missile Mod Rock"),
        impact = Api.SimpleUnit("COMBAT Unit Simple HitFx"),
        flight_time = 0.75,
        damage = function(attack_data, caster)
            return 88
        end,
    })
    character_data.attack_ids = {
        "mod_attack_rock",
        "mod_attack_rock",
        "mod_attack_rock",
        "mod_attack_rock",
        "mod_attack_rock",
        "mod_attack_rock",
    }
end

MOD_MANAGER:AddMod(loader)
